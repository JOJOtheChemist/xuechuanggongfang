# 豆包 lite 切换与 webresearch 测试结果

测试时间：2026-05-20

## 2026-05-20 二次复测更新

你在 2026-05-20 刚开通 `Doubao-Seed-2.0-lite 260428` 后，我重新做了一轮验证。

本轮最新结论：

1. `doubao-seed-2-0-lite-260428` 普通对话 smoke 已成功
2. 官方 `Responses API + web_search` 仍未成功，但这次报错已经变成 `ToolNotOpen`
3. 这说明模型本身已经开通，当前只差 `web search` 工具权限

### 二次复测 1：普通对话 smoke

测试命令：

```bash
cd /Users/yeya/Documents/HBuilderProjects/云创工坊/学创工坊后端/kode-sdk-xuechuang
zsh -lc 'set -a; source xuechuang.env; set +a; npx ts-node server/test-api-connection.ts'
```

结果：成功

关键结果：

- `complete` 请求成功
- `stream` 请求成功
- 模型回复 `测试成功`

这说明截至 2026-05-20，`doubao-seed-2-0-lite-260428` 已可作为当前项目默认模型使用。

### 二次复测 2：官方 webresearch / web_search

测试接口：

- `POST https://ark.cn-beijing.volces.com/api/v3/responses`

测试模型：

- `doubao-seed-2-0-lite-260428`

结果：失败

返回错误：

```json
{
  "error": {
    "code": "ToolNotOpen",
    "type": "NotFound"
  }
}
```

结论：

- 这次已经不是 `ModelNotOpen`
- 说明模型已经开通成功
- 当前真正未开通的是官方 `web search` 工具能力
- 方舟返回的开通提示链接是 `https://console.volcengine.com/common-buy/CC_content_plugin`

## 本次改动

已把项目里的模型配置从硅基流动切到火山方舟：

- 文件：`/Users/yeya/Documents/HBuilderProjects/云创工坊/学创工坊后端/kode-sdk-xuechuang/xuechuang.env`
- 文件：`/Users/yeya/Documents/HBuilderProjects/云创工坊/学创工坊后端/kode-sdk-xuechuang/.env`
- API Key：使用你提供的 `ark-7be76502-b85e-4ea4-9393-6e1f4ce715c3-f3946`
- Base URL：`https://ark.cn-beijing.volces.com/api/v3/chat/completions`
- 当前最终落地模型：`doubao-seed-2-0-lite-260428`

说明：

- 我先试了你提到的更便宜新模型 `doubao-seed-2-0-lite-260215`
- 结果该 key 调用时返回 `ModelNotOpen`
- 后续你又开通了 `doubao-seed-2-0-lite-260428`
- 截至 2026-05-20 二次复测，当前配置已切换为 `doubao-seed-2-0-lite-260428`

## Smoke 对话测试

测试命令：

```bash
cd /Users/yeya/Documents/HBuilderProjects/云创工坊/学创工坊后端/kode-sdk-xuechuang
zsh -lc 'set -a; source xuechuang.env; set +a; npx ts-node server/test-api-connection.ts'
```

结果：成功

关键结果：

- `complete` 请求成功
- `stream` 请求成功
- 模型回复 `测试成功`

测试中确认可用的模型配置：

```env
ANTHROPIC_API_KEY=ark-7be76502-b85e-4ea4-9393-6e1f4ce715c3-f3946
ANTHROPIC_MODEL_ID=doubao-seed-2-0-lite-260428
ANTHROPIC_BASE_URL=https://ark.cn-beijing.volces.com/api/v3/chat/completions
```

## 豆包官方 webresearch / web_search 测试

我测的是火山方舟官方 `Responses API` 的联网搜索能力，接口：

- `POST https://ark.cn-beijing.volces.com/api/v3/responses`

### 测试 1：`doubao-seed-2-0-lite-260215`

请求结果：失败

返回错误：

```json
{
  "error": {
    "code": "ModelNotOpen",
    "type": "Not Found"
  }
}
```

结论：

- 这把 key 虽然能在 `/models` 列表里看到 `doubao-seed-2-0-lite-260215`
- 但账号侧没有开通这个模型
- 所以当前不能用它做普通对话，也不能用它测官方联网搜索

### 测试 2：`doubao-1-5-lite-32k-250115`

普通对话结果：成功

官方 `Responses API + web_search` 结果：失败

返回错误：

```json
{
  "error": {
    "code": "AccessDenied",
    "type": "Forbidden"
  }
}
```

结论：

- 这个 lite 模型当前可以走 `chat/completions`
- 但没有 `responses api` 权限
- 所以不能直接用它测试官方 `web_search`

### 测试 3：`doubao-seed-2-0-lite-260428`

普通对话结果：成功

官方 `Responses API + web_search` 结果：失败

返回错误：

```json
{
  "error": {
    "code": "ToolNotOpen",
    "type": "NotFound"
  }
}
```

结论：

- 模型本身已经可以正常对话
- 但账号还没有开通 `web search` 工具权限
- 当前距离“官方联网搜索成功”只差工具权限这一步

## 额外排查

我还做了两类补充检查：

1. 拉取 `GET /api/v3/models`
- 确认这把 key 可以看到豆包多个模型，包括：
- `doubao-1-5-lite-32k-250115`
- `doubao-seed-2-0-lite-260215`
- `doubao-seed-2-0-mini-260215`
- `doubao-seed-2-0-lite-260428`

2. 尝试其他豆包新模型
- `doubao-seed-1-6-251015`
- 结果同样是 `ModelNotOpen`

说明当前阻塞点主要不是代码，而是账号工具权限状态。

## 最终结论

当前状态可以明确分成两部分：

1. 项目切到豆包已经完成，并且可正常对话
- 已切到火山方舟
- 当前可用模型为 `doubao-seed-2-0-lite-260428`
- smoke 对话成功

2. 豆包官方 webresearch 这次没有测通
- `seed-2.0-lite`：账号未开通
- `seed-2.0-lite-260428`：模型已开通，但 `web search` 工具未开通
- `1.5-lite`：无 `Responses API` 权限
- 所以本次无法在你这把 key 上验证“官方联网搜索成功”

## 建议下一步

如果你要继续推进官方联网能力，建议按这个顺序处理：

1. 在火山方舟控制台开通 `web search` 工具权限
2. 确认 `doubao-seed-2-0-lite-260428` 对当前账号允许 `Responses API + web_search`
3. 再用同一套 key 复测 `web_search`

如果你只想先让项目稳定跑起来，当前配置已经可以先用：

```env
ANTHROPIC_MODEL_ID=doubao-seed-2-0-lite-260428
```
